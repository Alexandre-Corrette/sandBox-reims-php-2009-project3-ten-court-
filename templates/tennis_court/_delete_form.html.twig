<form method="post" action="{{ path('tennis_court_delete', {'id': tennis_court.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tennis_court.id) }}">
    <button class="btn">Delete</button>
</form>
